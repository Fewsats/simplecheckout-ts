{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cd playground && npm install"
  },
  "deploy": {
    "startCommand": "cd playground && npm start"
  }
}