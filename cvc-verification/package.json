{
  "name": "simplecheckout-cvc-verification",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run build && npx serve dist -p ${PORT:-3000}"
  },
  "dependencies": {
    "simplecheckout-sdk": "^1.3.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
} 